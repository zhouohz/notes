## data()

数据缓存方法。该方法可以在一个元素内存放一个键值对，就像添加一个属性一样，但是这个属性不能在HTML结构中看到，是存放在元素的内存中的。而且这个属性的值也只能通过data()方法获取。

```javascript
// 设置
$('div').data('自定义属性名', '值')
// 获取
$('div').data('自定义属性名')
```

还有 该方法也能获取H5中的自定义属性，即 data-xxx 的属性。

```javascript
$('div').data('xxx')
```

不需要写前面的data-前缀

如果值为数字，则会得到num类型的结果

不过这个方法无法获取固定属性和其他形式的自定义属性